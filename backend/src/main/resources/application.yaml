spring:
  application:
    name: easy-restaurant
  datasource:
    url: jdbc:postgresql://localhost:5432/easy_restaurant_db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        show_sql: false

  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog-master.yaml
    drop-first: true

  jwt:
    secret: a-very-long-super-secure-secret-key-with-at-least-64-characters-!!!!
    expiration: 25000 # 30 SEG Expiration
    refresh-token-expiration: 900000 # 15 minExpiration

